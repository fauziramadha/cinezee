# Production environment variables
# File ini di-commit ke repo (hanya berisi domain publik, BUKAN secret)
# Next.js baca file ini saat `next build` (NODE_ENV=production)
# dan embed NEXT_PUBLIC_* ke client-side JS bundle

NEXT_PUBLIC_API_URL=https://api.cinestream.biz.id
